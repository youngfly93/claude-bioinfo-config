---
name: bio-docs-tidy
description: >-
  收口项目里散落、多版本、互相矛盾的 md 记录——找出当前真源，其余归档进一个 _archive/，
  产出 DOCS_INDEX.md（真源清单），消除"哪个 md 才是对的"误解。只归档不删除、单项目内、绝不跨合作者目录。
  触发条件：用户说"收口"、"整理文档"、"docs-tidy"、"md 太多了/太乱"、"去重 md"、
  "哪个是最新的"、"清理冗余文档"、"建文档索引"、"这堆 md 看晕了"。
  不适用于：删除数据/结果文件（只动 md 类记录）、交付打包（用 bio-deliver）、
  从零理解项目（用 bio-zoom-out）。
---

# 文档收口去冗

agentic 分析会不断写 md（审计记录、说明、各版本草稿），堆久了多版本并存、互相矛盾，看的人不知哪个算数。本 skill 把它们**收口成单一真源 + 一份索引**。

> 原则见全局/工作区 CLAUDE.md「文档卫生」：一事一文件、原地改、版本交 git。本 skill 治理**存量**散落。

## 硬边界（先记住）

- **只归档，不删除**：旧版/冗余一律 `mv` 进**一个** `_archive/`，绝不 `rm`（用户另说"确认删"才删）。
- **只在单个项目目录内**：绝不跨顶层合作者目录（全局纪律：别动别人的目录）。动手前确认范围。
- **真源拿不准就问**：哪个是当前权威有疑问 → 列出来问用户，不擅自判定。
- 动手前 `git status`；过程稿（`.work/`）虽可丢，移动前也先列给用户看。

## 工作流

### 1. 限定范围
确认要收口的**单个项目目录**（默认当前目录，但不上溯到合作者根）。

### 2. 只读扫描
```bash
# 列 md + 标多版本/副本命名（按目录看散落程度）
find <项目> -name '*.md' ! -path '*/.git/*' ! -path '*/_archive/*' ! -name '._*' 2>/dev/null
# 多版本/副本命名命中
find <项目> -name '*.md' ! -path '*/.git/*' ! -name '._*' 2>/dev/null \
  | grep -iE '(_v[0-9]|_final|_最终|_new|_新|副本| copy|\([0-9]\)| [0-9]\.md|backup|bak|old)'
```
- 按**主题/文件名词干**聚类（如所有 `AUDIT_*`、所有 `*plan*`、所有 `*methods*`）。
- 每组按修改时间 + 是否 git 跟踪 + 内容完整度排序。

### 3. 判定当前真源
每组挑"当前权威"，依据（按优先级）：
- 文件名最干净（无 `_v*/_final/副本` 后缀）且 git 跟踪的；
- 内容最新、最全；与 `plan.md`/`HANDOFF.md` 指向一致的。
- 其余归为「旧版 / 过程稿」。**拿不准 → 列出问用户**。

### 4. 给方案、等确认
输出收口方案表，确认后才动：

```markdown
## 文档收口方案（<项目>）
| 主题 | 当前真源(保留) | 拟归档进 _archive/ | 依据 |
|------|---------------|-------------------|------|
| 审计 | audit/AUDIT.md | AUDIT_*_FINAL.md ×3、v1.2.* ×4 | 后者为历史快照 |
```

### 5. 执行（确认后）
- 旧版/过程稿 `mv` 进项目根**一个** `_archive/`（保留，不删）。
- 真源留原位、文件名规范化（去掉 `_final` 等后缀时用 `git mv`）。

### 6. 产出真源清单 DOCS_INDEX.md
在项目根写/更新 `DOCS_INDEX.md`，让接手者一眼知道哪几个权威：

```markdown
# DOCS_INDEX · <项目>  (更新: <绝对日期>)
## 权威文档（认这些）
- plan.md — 设计与进度
- HANDOFF.md — 当前状态快照
- reports/<最终报告>.docx — 交付报告
- audit/AUDIT.md — 最新审计
## 历史（_archive/，仅备查，勿据此分析）
- 旧版审计 / 各版本草稿 …
```

## 纪律
- 只 `mv` 不 `rm`；归档目录只用一个 `_archive/`，别再造多个。
- 不跨合作者目录；不动 results/figures 等**数据**文件（只收 md 类记录）。
- 收口后提醒：以后**原地改真源**、新版交 git，别再堆文件名（`canonical_guard` 会拦多版本命名）。
